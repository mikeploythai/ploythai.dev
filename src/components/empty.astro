<div class="empty">
	<p class="empty__placeholder">
		<span class="empty__placeholder__icon">👀</span>
		<br />
		Soon&trade;
	</p>
</div>

<style>
	.empty {
		display: grid;
		place-items: center;
		padding: 1rem;
		border-width: 1px;
		border-style: dashed;
		border-radius: var(--radius-lg);
		min-block-size: 8rem;
		background-color: color-mix(in oklab, var(--accent), transparent 85%);
	}

	.empty__placeholder {
		font-family: var(--font-mono);
		text-align: center;
		font-style: italic;
	}

	.empty__placeholder__icon {
		font-size: 1.25rem;
		font-style: normal;
	}
</style>
